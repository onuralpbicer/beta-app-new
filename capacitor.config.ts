import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
    appId: 'com.beta.bakim.app',
    appName: 'beta-app-new',
    webDir: 'www',
    server: {
        androidScheme: 'https',
    },
}

export default config
